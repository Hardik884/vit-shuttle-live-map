# Backend

This folder contains the backend API, separated from the frontend app in the project root.

## Setup

1. Install dependencies:
   npm install
2. Start dev server:
   npm run dev

The backend runs by default on `http://localhost:5000`.

## Endpoints

- `GET /health` - health check
- `GET /api` - sample response
